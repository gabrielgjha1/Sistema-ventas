import { ImagenTraerPipe } from './imagen-traer.pipe';

describe('ImagenTraerPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenTraerPipe();
    expect(pipe).toBeTruthy();
  });
});
